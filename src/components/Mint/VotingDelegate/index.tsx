import Popup from "reactjs-popup";

const VotingDelegate = () => {
  return (
    <div>
      <h1>
        Since defifa is self-controlled game, we offer you an option to either
        just mint and chill till the end, or in case you want to attest, you are
        free to do so
      </h1>
    </div>
  );
};

export default VotingDelegate;
